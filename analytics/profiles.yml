analytics:
  outputs:
    dev:
      dbname: "{{ env_var('DB_NAME') }}"
      host: localhost
      pass:  "{{ env_var('DB_PASSWORD') }}"
      port: 5433
      schema: analytics
      threads: 4
      type: postgres
      user:  "{{ env_var('DB_USER') }}"
  target: dev
